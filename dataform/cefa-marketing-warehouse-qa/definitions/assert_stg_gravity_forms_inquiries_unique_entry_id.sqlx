config {
  type: "assertion",
  tags: ["existing_qa", "parent", "uniqueness"]
}

SELECT entry_id
FROM `marketing-api-488017.staging_marketing.stg_gravity_forms_inquiries`
GROUP BY entry_id
HAVING COUNT(*) > 1
